enum BookingAppointmentType { initial, followUp }

enum BookingProblem {
  offline,
  timeout,
  forbidden,
  sessionExpired,
  malformedResponse,
  backendUnavailable,
  validation,
  conflict,
  unknownOutcome,
  unknown,
}
