import{readFileSync,readdirSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const root=resolve(process.cwd(),"src"),read=(path:string)=>readFileSync(resolve(root,path),"utf8");describe("protected routing architecture",()=>{it("protects every operational route family on the server",()=>{for(const path of["app/[locale]/dashboard/layout.tsx","app/[locale]/patients/layout.tsx","app/[locale]/appointments/layout.tsx","app/[locale]/live-queue/layout.tsx","app/[locale]/doctor/layout.tsx","app/[locale]/[section]/layout.tsx"])expect(read(path),path).toMatch(/Protected(?:Layout|Route)/u)});it("keeps login outside the protected portal shell and tokens outside client props",()=>{expect(read("app/[locale]/layout.tsx")).not.toMatch(/PortalShell|ProtectedRoute/u);expect(read("features/authentication/presentation/login-form.tsx")).not.toMatch(/accessToken|refreshToken|sealedSession/u)});it("renders honest production placeholders without workflow mutations",()=>{const protectedRoute=read("features/portal-foundation/presentation/protected-route.tsx"),placeholder=read("features/portal-foundation/presentation/honest-placeholder.tsx");expect(protectedRoute).toContain('environment==="development"?children:<HonestPlaceholder');expect(placeholder).toMatch(/featureUnavailable|futureOwner/u);expect(placeholder).not.toMatch(/button|onClick|mutation/u)});it("keeps new foundation localization files free of mojibake",()=>{const directory=resolve(root,"features/portal-foundation/presentation");for(const entry of readdirSync(directory))if(entry.endsWith(".ts")||entry.endsWith(".tsx"))expect(readFileSync(resolve(directory,entry),"utf8"),entry).not.toMatch(/â|Ã|Ø|Ù|�/u)})});
