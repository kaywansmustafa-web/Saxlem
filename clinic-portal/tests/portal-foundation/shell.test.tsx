import{cleanup,render,screen}from"@testing-library/react";import userEvent from"@testing-library/user-event";import{axe}from"jest-axe";import{afterEach,describe,expect,it,vi}from"vitest";import{PortalShell}from"@/features/portal-foundation/presentation/portal-shell";import{foundationMessages}from"@/features/portal-foundation/presentation/foundation-messages";import{navigationFor}from"@/features/portal-foundation/domain/route-policy";afterEach(cleanup);describe("role shell",()=>{it("has accessible role-specific navigation and active semantics",async()=>{vi.stubGlobal("fetch",vi.fn());const{container}=render(<PortalShell locale="en" m={foundationMessages("en")} role="receptionist" current="dashboard" navigation={navigationFor("receptionist")} context={{organizationId:"33333333-3333-4333-8333-333333333333",clinicId:"44444444-4444-4444-8444-444444444444"}}><h1>Page</h1></PortalShell>);expect(screen.getByRole("link",{name:"Dashboard"})).toHaveAttribute("aria-current","page");expect(screen.queryByRole("link",{name:"Workspace"})).not.toBeInTheDocument();expect((await axe(container)).violations).toEqual([])});it("opens drawer, moves focus, traps focus, closes with Escape and restores trigger",async()=>{const user=userEvent.setup(),m=foundationMessages("ku");render(<PortalShell locale="ku" m={m} role="doctor" current="doctorWorkspace" navigation={navigationFor("doctor")} context={{}}><h1>Page</h1></PortalShell>);const trigger=screen.getByRole("button",{name:m.openMenu});await user.click(trigger);expect(screen.getAllByRole("link",{name:m.settings})).toHaveLength(1);expect(document.querySelector(".foundation-workspace")).toHaveAttribute("inert");expect(document.activeElement?.tagName).toMatch(/A|BUTTON/);await user.keyboard("{Escape}");expect(trigger).toHaveFocus()})});
