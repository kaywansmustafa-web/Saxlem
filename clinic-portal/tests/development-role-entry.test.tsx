import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";vi.mock("next/navigation",()=>({usePathname:()=>"/en/dashboard"}));import{Shell}from"@/components/shell";import{messages}from"@/i18n";afterEach(cleanup);describe("development role entry",()=>{it("shows the doctor entry only when the server grants development capability",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry><h1>Reception</h1></Shell>);expect(screen.getByRole("link",{name:"Open Doctor Workspace"})).toHaveAttribute("href","/en/doctor/session");expect(screen.getByRole("link",{name:"Doctors"})).toHaveAttribute("href","/en/doctors");expect(screen.queryByRole("link",{name:"Workspace"})).not.toBeInTheDocument()});it("does not expose the control when production-safe composition denies capability",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry={false}><h1>Reception</h1></Shell>);expect(screen.queryByRole("link",{name:"Open Doctor Workspace"})).not.toBeInTheDocument();expect(screen.getByRole("link",{name:"Dashboard"})).toBeInTheDocument()})});
