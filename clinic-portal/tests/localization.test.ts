import{describe,it,expect}from"vitest";import{messages,dir,fmt}from"@/i18n";describe("localization",()=>{it("keeps message key parity",()=>{expect(Object.keys(messages("ar"))).toEqual(Object.keys(messages("en")));expect(Object.keys(messages("ku"))).toEqual(Object.keys(messages("en")))});it("uses RTL for Arabic and Badini",()=>{expect(dir("ar")).toBe("rtl");expect(dir("ku")).toBe("rtl")});it("interpolates localized placeholders",()=>{expect(fmt(messages("en").late,{minutes:12})).toBe("Running 12 minutes late");expect(fmt(messages("en").count,{count:8})).toBe("8 patients")})});
