import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";vi.mock("next/navigation",()=>({usePathname:()=>"/en/doctor/settings"}));import{Shell}from"@/components/shell";import{messages}from"@/i18n";afterEach(cleanup);describe("doctor settings shell integration",()=>{it("marks Settings current and preserves doctor-only navigation",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry><h1>Settings content</h1></Shell>);expect(screen.getByRole("link",{name:"Settings"})).toHaveAttribute("aria-current","page");expect(screen.getByRole("link",{name:"Workspace"})).toBeInTheDocument();expect(screen.queryByRole("link",{name:"Dashboard"})).not.toBeInTheDocument();expect(screen.queryByRole("link",{name:"Live Queue"})).not.toBeInTheDocument()})});
