import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";vi.mock("next/navigation",()=>({usePathname:()=>"/en/doctor/settings"}));import{Shell}from"@/components/shell";import{messages}from"@/i18n";afterEach(cleanup);describe("doctor development role control placement",()=>{it("places the approved development return inside the doctor header",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry><h1>Settings</h1></Shell>);const control=screen.getByRole("link",{name:"Return to Receptionist Portal"});expect(control.closest("header")).toHaveClass("topbar");expect(control).toHaveAttribute("href","/en/dashboard")});it("does not render the control without development capability",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry={false}><h1>Settings</h1></Shell>);expect(screen.queryByRole("link",{name:"Return to Receptionist Portal"})).not.toBeInTheDocument()})});
