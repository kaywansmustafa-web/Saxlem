import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";vi.mock("next/navigation",()=>({usePathname:()=>"/en/doctor/schedule"}));import{Shell}from"@/components/shell";import{messages}from"@/i18n";afterEach(cleanup);describe("doctor schedule shell integration",()=>{it("uses doctor-only navigation and marks Schedule current",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry><h1>Schedule content</h1></Shell>);expect(screen.getByRole("link",{name:"Schedule"})).toHaveAttribute("aria-current","page");expect(screen.getByRole("link",{name:"Workspace"})).toBeInTheDocument();expect(screen.queryByRole("link",{name:"Dashboard"})).not.toBeInTheDocument();expect(screen.queryByRole("link",{name:"Live Queue"})).not.toBeInTheDocument()})});
