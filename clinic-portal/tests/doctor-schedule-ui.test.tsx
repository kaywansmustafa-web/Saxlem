import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it}from"vitest";import{MockAppointmentRepository}from"@/features/appointments/data/mock-appointment-repository";import{GetDoctorSchedule}from"@/features/doctor-schedule/application/get-doctor-schedule";import{MockDoctorScheduleRepository}from"@/features/doctor-schedule/data/mock-doctor-schedule-repository";import{DoctorScheduleView}from"@/features/doctor-schedule/presentation/doctor-schedule-view";import{doctorScheduleMessages}from"@/features/doctor-schedule/presentation/messages";import{MockDoctorSessionRepository}from"@/features/doctor/data/mock-doctor-session-repository";import{MockLiveQueueRepository}from"@/features/live-queue/data/mock-live-queue-repository";afterEach(cleanup);const projection=()=>new GetDoctorSchedule(new MockDoctorScheduleRepository(),new MockAppointmentRepository(),new MockDoctorSessionRepository(),new MockLiveQueueRepository()).execute("session-karwan-morning");describe("doctor schedule presentation",()=>{it("highlights the current consultation and renders breaks with semantic headings",async()=>{const data=await projection();if(!data)throw Error("missing schedule");const{container}=render(<DoctorScheduleView data={data} locale="en" m={doctorScheduleMessages("en")}/>);expect(screen.getByRole("heading",{name:"Today's Timeline"})).toBeInTheDocument();expect(container.querySelector(".schedule-timeline li.current")).toHaveAttribute("aria-current","true");expect(screen.getAllByText("Lunch break").length).toBeGreaterThan(0);expect(screen.queryByRole("searchbox")).not.toBeInTheDocument()});it("supports RTL and keeps locale catalogs in parity",async()=>{const data=await projection();if(!data)throw Error("missing schedule");render(<div dir="rtl"><DoctorScheduleView data={data} locale="ar" m={doctorScheduleMessages("ar")}/></div>);expect(screen.getByRole("heading",{name:"جدول الطبيب",level:1})).toBeInTheDocument();expect(screen.getByRole("link",{name:"العودة إلى مساحة العمل"})).toHaveAttribute("href","/ar/doctor/session");expect(Object.keys(doctorScheduleMessages("ar"))).toEqual(Object.keys(doctorScheduleMessages("en")));expect(Object.keys(doctorScheduleMessages("ku"))).toEqual(Object.keys(doctorScheduleMessages("en")))})});
