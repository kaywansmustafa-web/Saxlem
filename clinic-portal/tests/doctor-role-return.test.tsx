import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";vi.mock("next/navigation",()=>({usePathname:()=>"/en/doctor/session"}));import{Shell}from"@/components/shell";import{messages}from"@/i18n";afterEach(cleanup);describe("doctor development return path",()=>{it("offers a development return without merging receptionist navigation",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry><h1>Doctor</h1></Shell>);expect(screen.getByRole("link",{name:"Return to Receptionist Portal"})).toHaveAttribute("href","/en/dashboard");expect(screen.getByRole("link",{name:"Workspace"})).toBeInTheDocument();expect(screen.queryByRole("link",{name:"Dashboard"})).not.toBeInTheDocument();expect(screen.queryByRole("link",{name:"Live Queue"})).not.toBeInTheDocument()});it("hides the return path without development capability",()=>{render(<Shell locale="en" m={messages("en")} allowDoctorEntry={false}><h1>Doctor</h1></Shell>);expect(screen.queryByRole("link",{name:"Return to Receptionist Portal"})).not.toBeInTheDocument()})});
