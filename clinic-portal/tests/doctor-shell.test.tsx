import{cleanup,render,screen}from"@testing-library/react";import{afterEach,describe,expect,it,vi}from"vitest";vi.mock("next/navigation",()=>({usePathname:()=>"/en/doctor/session"}));import{Shell}from"@/components/shell";import{messages}from"@/i18n";afterEach(cleanup);describe("doctor role shell",()=>{it("exposes only five doctor destinations and no receptionist routes",()=>{render(<Shell locale="en" m={messages("en")}><h1>Doctor content</h1></Shell>);expect(screen.getByRole("link",{name:"Workspace"})).toHaveAttribute("aria-current","page");expect(screen.getByRole("link",{name:"Patients Today"})).toBeInTheDocument();expect(screen.queryByRole("link",{name:"Dashboard"})).not.toBeInTheDocument();expect(screen.queryByRole("link",{name:"Live Queue"})).not.toBeInTheDocument();expect(screen.getAllByRole("link").filter(link=>link.closest("nav"))).toHaveLength(5)})});
