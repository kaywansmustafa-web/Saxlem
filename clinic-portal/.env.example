SAXLEM_PORTAL_ENV=development
SAXLEM_BACKEND_API_URL=http://localhost:3000
# Intentionally invalid. Generate a value with:
# node -e "console.log(require('node:crypto').randomBytes(32).toString('base64url'))"
SAXLEM_PORTAL_SESSION_SECRET=<generate-a-32-byte-base64url-secret>
SAXLEM_PORTAL_REQUEST_TIMEOUT_MS=8000
