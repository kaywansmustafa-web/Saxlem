import{notFound}from"next/navigation";import{isLocale}from"@/i18n";import{sectionRoute}from"@/features/portal-foundation/domain/route-policy";import{ProtectedRoute}from"@/features/portal-foundation/presentation/protected-route";export default async function Layout({children,params}:{children:React.ReactNode;params:Promise<{locale:string;section:string}>}){const{locale,section}=await params,route=sectionRoute(section);if(!isLocale(locale)||!route)notFound();return <ProtectedRoute locale={locale} route={route}>{children}</ProtectedRoute>}
