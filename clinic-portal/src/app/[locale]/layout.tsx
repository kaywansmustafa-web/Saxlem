import{notFound}from"next/navigation";import{dir,isLocale,messages}from"@/i18n";import{Shell}from"@/components/shell";export default async function LocaleLayout({children,params}:{children:React.ReactNode;params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const m=messages(locale);return <div lang={locale} dir={dir(locale)}><Shell locale={locale} m={m}>{children}</Shell></div>}
