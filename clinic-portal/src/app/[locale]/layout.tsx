import{notFound}from"next/navigation";import{dir,isLocale}from"@/i18n";export default async function LocaleLayout({children,params}:{children:React.ReactNode;params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();return <div className={`locale-root locale-${locale}`} lang={locale} dir={dir(locale)}>{children}</div>}
