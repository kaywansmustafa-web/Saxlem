import Link from"next/link";import{notFound}from"next/navigation";import{isLocale}from"@/i18n";import{doctorMessages}from"@/features/doctor/presentation/messages";import{getDoctorSession}from"@/infrastructure/composition";const allowed=["patients","schedule","notifications","settings"];export default async function Page({params}:{params:Promise<{locale:string;destination:string}>}){const{locale,destination}=await params;if(!isLocale(locale)||!allowed.includes(destination)||!getDoctorSession())notFound();const m=doctorMessages(locale);return <div className="state"><div><h1>{m.placeholderTitle}</h1><p>{m.placeholderBody}</p><Link className="link" href={`/${locale}/doctor/session`}>{m.workspace}</Link></div></div>}
