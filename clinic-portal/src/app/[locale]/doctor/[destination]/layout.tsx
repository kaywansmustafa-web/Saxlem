import{notFound}from"next/navigation";import{isLocale}from"@/i18n";import{ProtectedRoute}from"@/features/portal-foundation/presentation/protected-route";export default async function Layout({children,params}:{children:React.ReactNode;params:Promise<{locale:string;destination:string}>}){const{locale,destination}=await params;if(!isLocale(locale)||destination!=="patients")notFound();return <ProtectedRoute locale={locale} route="doctorPatients">{children}</ProtectedRoute>}
