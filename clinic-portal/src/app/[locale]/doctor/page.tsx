import{redirect,notFound}from"next/navigation";import{isLocale}from"@/i18n";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();redirect(`/${locale}/doctor/session`)}
