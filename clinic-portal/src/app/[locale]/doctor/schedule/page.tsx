import{notFound}from"next/navigation";import{isLocale}from"@/i18n";import{doctorScheduleServices}from"@portal-composition";import{DoctorScheduleView}from"@/features/doctor-schedule/presentation/doctor-schedule-view";import{doctorScheduleMessages}from"@/features/doctor-schedule/presentation/messages";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const m=doctorScheduleMessages(locale),services=doctorScheduleServices();if(!services)return <div className="state"><div><h1>{m.unavailable}</h1><p>{m.unavailableBody}</p></div></div>;const data=await services.get.execute(services.session.id);if(!data)return <div className="state"><div><h1>{m.unavailable}</h1><p>{m.unavailableBody}</p></div></div>;return <DoctorScheduleView data={data} locale={locale} m={m}/>}
