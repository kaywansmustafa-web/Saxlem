import{notFound}from"next/navigation";import{isLocale}from"@/i18n";import{doctorMessages}from"@/features/doctor/presentation/messages";import{DoctorWorkspace}from"@/features/doctor/presentation/doctor-workspace";import{doctorServices}from"@portal-composition";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const services=doctorServices();if(!services)return <div className="state"><div><h1>{doctorMessages(locale).unavailable}</h1><p>{doctorMessages(locale).unavailableBody}</p></div></div>;const data=await services.get.execute(services.session.id);if(!data)notFound();return <DoctorWorkspace initial={data} locale={locale} m={doctorMessages(locale)}/>}
