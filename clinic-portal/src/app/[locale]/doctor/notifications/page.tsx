import{notFound}from"next/navigation";import{isLocale}from"@/i18n";import{doctorNotificationServices,doctorServices}from"@portal-composition";import{DoctorNotificationsWorkspace}from"@/features/doctor-notifications/presentation/doctor-notifications-workspace";import{doctorNotificationMessages}from"@/features/doctor-notifications/presentation/messages";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const m=doctorNotificationMessages(locale),services=doctorNotificationServices(),doctor=doctorServices();if(!services||!doctor)return <div className="state"><div><h1>{m.unavailable}</h1><p>{m.unavailableBody}</p></div></div>;const data=await services.get.execute(services.session.doctorId,services.session.clinicId),workspace=await doctor.get.execute(doctor.session.id);return <DoctorNotificationsWorkspace initial={data} locale={locale} m={m} currentPatientId={workspace?.current?.patientId}/>}
