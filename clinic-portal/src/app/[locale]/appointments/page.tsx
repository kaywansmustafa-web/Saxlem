import{notFound}from"next/navigation";import{isLocale,appointmentMessages}from"@/i18n";import{appointmentServices}from"@portal-composition";import{AppointmentsPage}from"@/features/appointments/presentation/appointments-page";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const service=appointmentServices();if(!service)notFound();return <AppointmentsPage items={await service.list.execute()} locale={locale} m={appointmentMessages(locale)}/>}
