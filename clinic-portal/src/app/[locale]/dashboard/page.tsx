import{notFound}from"next/navigation";import{dashboardService,getSession}from"@/infrastructure/composition";import{isLocale,messages}from"@/i18n";import{DashboardView}from"@/features/dashboard/presentation/dashboard-view";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const m=messages(locale),session=getSession(),service=dashboardService();if(!session||!service)return <div className="state"><div><h1>{m.unavailable}</h1><p>{m.unavailableBody}</p></div></div>;const data=await service.execute(session.clinicId);return <DashboardView data={data} m={m} locale={locale}/>}
