import{notFound}from"next/navigation";import{isLocale,patientMessages}from"@/i18n";import{patientServices}from"@/infrastructure/composition";import{PatientsPage}from"@/features/patients/presentation/patients-page";export default async function Page({params}:{params:Promise<{locale:string}>}){const{locale}=await params;if(!isLocale(locale))notFound();const services=patientServices();if(!services)notFound();return <PatientsPage patients={await services.list.execute()} locale={locale} m={patientMessages(locale)}/>}
