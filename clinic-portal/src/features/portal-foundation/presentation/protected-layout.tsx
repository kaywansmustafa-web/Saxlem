import { notFound } from "next/navigation";import{isLocale}from"@/i18n";import type{PortalRouteId}from"../domain/route-policy";import{ProtectedRoute}from"./protected-route";export async function ProtectedLayout({children,params,route}:{children:React.ReactNode;params:Promise<{locale:string}>;route:PortalRouteId}){const{locale}=await params;if(!isLocale(locale))notFound();return <ProtectedRoute locale={locale} route={route}>{children}</ProtectedRoute>}
