import type{DoctorNotification,MarkNotificationReadResult}from"./models";export interface DoctorNotificationRepository{list(doctorId:string,clinicId:string):Promise<{version:number;lastUpdated:string;notifications:DoctorNotification[]}>;markRead(input:{doctorId:string;clinicId:string;notificationId:string;expectedVersion:number;operationId:string;readAt:string}):Promise<MarkNotificationReadResult>}
