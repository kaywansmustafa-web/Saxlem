# OpenAPI

`saxlem-api.json` is generated with `npm run openapi:generate` and validated with
`npm run openapi:validate`. Generated clients must remain infrastructure details
in Flutter and the clinic portal; they must not replace frontend domain models.
