# Clinic Portal Routing

Routes begin with `[locale]` for `en`, `ar`, or `ku`. The root redirects to `/en/dashboard`. Dashboard is functional. Patients, Appointments, Live Queue, Doctors, Schedule, Notifications, and Settings share an honest placeholder boundary. Reports does not exist. The locale layout owns direction and the receptionist shell; route selection supplies `aria-current` navigation state.
